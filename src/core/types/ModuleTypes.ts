/**
 * Enum defining the types of modules supported by the system
 */
export enum ModuleType {
  PERSONALITY = 'personality',
  TOOL = 'tool',
  AGENT = 'agent',
  THEME = 'theme',
  MODALITY = 'modality',
}
