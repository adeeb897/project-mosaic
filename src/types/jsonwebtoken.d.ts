// This file extends the jsonwebtoken types if needed
// Currently, the official @types/jsonwebtoken package provides all necessary types
// so we don't need to override anything here.

// If you need to extend the JwtPayload interface in the future, you can do:
// declare module 'jsonwebtoken' {
//   interface JwtPayload {
//     // Add custom properties here
//   }
// }
